extends partials/layout

block content
	div( ng-controller="account" )
		.l-mt-20
			h2.m-fz-20 アカウント登録／ログイン
		.l-clr.l-mt-10.l-mb-10
			.l-left.l-flex.ss-flex-center
				div( ng-if='isRegist' )
					|新規アカウント登録
				div( ng-if='!isRegist' )
					|ログイン
		
		form
			.form-group
				label
					|メールアドレス
				input.form-control( required type='email', placeholder='メールアドレス', ng-model='mail' )
			.form-group
				label
					|パスワード
				input.form-control( required type='password', placeholder='パスワード', ng-model='password' )
		
			div( ng-if='isRegist' )
				button( type='submit' ng-click='regist( mail, password )' ).btn.btn-default 新規登録
				div( ng-click='toggleBtn()' ).btn.btn-link
					|アカウント登録済みの方はこちら
		
			div( ng-if='!isRegist' )
				button( type='submit' ng-click='login( mail, password )' ).btn.btn-default ログイン
				div( ng-click='toggleBtn()' ).btn.btn-link
					|新規にアカウントを登録する方はこちら
		
			p( ng-if='isSending' class="l-mt-5" )
				|{{ statusMessage }}
			
	include partials/description
	
	.l-mt-50
		h2.m-fz-20
			|Kindle化されている書籍を検索する
		include partials/search
