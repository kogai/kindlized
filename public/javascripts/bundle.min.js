/**
 * Modules in this bundle
 * 
 * kindlized:
 *   license: ISC
 *   author: https://twitter.com/iamchawan
 * 
 */
(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
var app;

app = require('./lib/app');

angular.module('App', []).controller('postNewBook', ['$scope', '$filter', '$http', app]);



},{"./lib/app":2}],2:[function(require,module,exports){
module.exports = function($scope, $filter, $http) {
  var httpOpt;
  httpOpt = {
    method: 'get',
    url: '/book'
  };
  $http(httpOpt).success(function(bookListInDB, status) {
    return $scope.bookListInDB = bookListInDB;
  }).then(function() {});
  $scope.searchBook = function() {
    console.log($scope.newBook, 'postAuthor clicked');
    $scope.isNotExistenceBook = false;
    $scope.isBookAdded = false;
    $scope.showSuggestedBooks = false;
    $scope.isWaiting = true;
    httpOpt = {
      method: 'post',
      url: '/post',
      data: {
        newBook: $scope.newBook
      }
    };
    $http(httpOpt).success(function(data, status) {
      $scope.isNotExistenceBook = data.isNotExistenceBook;
      $scope.showSuggestedBooks = true;
      $scope.bookListInAmazon = data.bookListInAmazon;
      $scope.isWaiting = false;
    }).then(function() {
      console.log('author saved.');
      $scope.newBook = '';
    });
  };
  $scope.registBook = function(newBook) {
    console.log(newBook);
    httpOpt = {
      method: 'post',
      url: '/save',
      data: {
        newBook: newBook
      }
    };
    $http(httpOpt).success(function(data, status) {
      console.log('registBook satatus is', status);
    }).then(function() {
      console.log('author saved.');
      $scope.newBook = '';
    });
  };
};



},{}]},{},[1]);
