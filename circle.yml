machine:
  services:
    - docker
  timezone: Asia/Tokyo

dependencies:
  override:
    - docker info
    - docker build -t kindlized-client ./client
    - docker build -t kindlized-server ./server

# test:
#   override:
#     - bundle
#     - bundle exec rspec

# deployment:
#   staging:
#     branch: master
#     commands:
#       - rm -rf .tmp
